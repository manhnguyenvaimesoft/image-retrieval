# Backend User Guide

## Configuration Setup:

- Create a project in the projects directory, set vector.index and image_paths.json (if applicable). If not, these two files will be automatically generated from the database.

## Run

- Run API: `uvicorn main:app --host 0.0.0.0 --port 8000 --reload`
